@model Fjordens_Service_ver2.Models.Employee

@{
    ViewBag.Title = "Update";
}
<div class="container">
    <div class="row">
        <h2 class="text-center">Rediger</h2>
    </div>
    <div class="row">
        <div class="col-lg-4 col-lg-offset-4 col-md-4 col-md-offset-4 col-sm-4 col-md-offset-4 form-con">
            <hr />
            @using (Html.BeginForm("Update", "Employee", FormMethod.Post))
            {
                @Html.ValidationSummary(true)
                @Html.HiddenFor(m => m.EmployeeId)
                @Html.Partial("_CreateEmployeePartial", Model)
                <input type="submit" value="Gem" />
            }
        </div>
    </div>

</div>
